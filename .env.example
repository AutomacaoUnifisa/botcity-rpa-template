# Copy this file to `.env` and fill in the values.
# Required only for `--environment local`: in maestro mode the BotCity agent
# passes these to the SDK via command-line arguments.
SERVER_MAESTRO="your_maestro_server_url"
LOGIN_MAESTRO="your_maestro_login"
KEY_MAESTRO="your_maestro_key"
